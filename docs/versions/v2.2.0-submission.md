# v2.2.0-submission: the version submitted to the HP Edge AI SJSU Hackathon

**Tag:** `v2.2.0-submission` · **Date:** 2026-09-26 · **Status:** frozen · **Current release**

This builds on [v2.1.0-officer-console](v2.1.0-officer-console.md), with the same models and weights ([v1.3.0-joint](v1.3.0-joint.md) detector and the `context` LoRA; `context_v2` selectable).

## Added since v2.1.0

| What | Where | Verified on the Nano |
|---|---|---|
| **Live pipeline race:** one fire frame through edge and cloud-only, phase by phase, with that frame's real timings | Edge vs Cloud tab | Online: edge 5.4 s, frame stays local; cloud 5.9 s after a 58 KB upload. Outage: edge queues, cloud blind. Restore: queued → restored → sent |
| **Live camera stage:** the video on the right; its real frames drive the pipeline on the left; team-phone (ntfy) panel | Mobile camera tab | Wildfire video: smoke 1/3 → 3/3 → VLM 5.7 s → ALERT → delivered; outage: queued, cloud blind |
| **Guided scenarios:** the old "Demo guide" renamed; sample photos | Officer console | — |
| **Docker:** `Dockerfile` (arm64, CUDA 13 PyTorch, the VLM in HP zrt via its socket) and `docker-compose.yml` (mounts, GPU, runs as the Nano user) | repository root | `docker compose config` validated; first build to be run by the owner (needs Docker permissions) |
| **Results:** LoRA v2 training and evaluation (source type 74.4%, group 77.0%); YOLO11m joint detector (0.658, not adopted) | `results/` | — |

608 tests pass. The runbook is [docs/DEMO_RUNBOOK.md](../DEMO_RUNBOOK.md); screens are in `results/console/`.

## Restore

`git checkout v2.2.0-submission` (weights as [v1.3.0-joint](v1.3.0-joint.md)).
